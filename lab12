#include <stdio.h>
#include <stdlib.h>
int main(int argc, char* argv[]) {
    FILE *out;
    out = fopen("out.txt", "w");
    int a = atoi(argv[1]);
    int b = atoi(argv[2]);
    fprintf(out, "%d", a + b);
    fclose(out);
}
